{
  "name": "partypilot",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build && pnpm --filter api build",
    "start": "pnpm --filter web start"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
