{
  "name": "partypilot",
  "version": "1.0.0",
  "private": true,
  "description": "PartyPilot - Natural language event planning and booking system",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @partypilot/web dev",
    "build": "pnpm --filter @partypilot/web build",
    "start": "pnpm --filter @partypilot/web start"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
